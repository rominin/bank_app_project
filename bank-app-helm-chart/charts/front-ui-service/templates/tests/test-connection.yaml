apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "front-ui-service.fullname" . }}-test-connection"
  labels:
    {{- include "front-ui-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "front-ui-service.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never