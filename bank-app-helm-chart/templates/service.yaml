apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-app-helm-chart.fullname" . }}
  labels:
    {{- include "bank-app-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-app-helm-chart.selectorLabels" . | nindent 4 }}
